import AppHeader from "../AppHeader/AppHeader";
import Filters from "../Filters/Filters";

const App = () => {
   return (
      <>
         <AppHeader />
         <main>
            <Filters />
         </main>
      </>
   );
};

export default App;
